# R64 测试报告

> **轮次：** R64 — F-21 Gateway `mention_keyword` 多触发词支持
> **测试者：** 泰虾（QA）
> **代码 Commit：** `7aeb824`（含编码 + 长词排序 + 审查报告）
> **测试日期：** `date`

## 测试结论 🎉

**✅ 全部通过（9/9）**

## 测试矩阵

| # | 场景 | 输入 | 预期 | 结果 |
|:-:|:-----|:-----|:-----|:----:|
| 1 | 单值向下兼容 | `"小开".split(";")` | `["小开"]` | ✅ |
| 2 | 多值 parse | `"小开;arch".split(";")` | `["小开", "arch"]` | ✅ |
| 3 | 空格 trim | `" 小开 ; arch "` | `["小开", "arch"]` | ✅ |
| 4 | 长词优先排序 | `"小;小开;admin"` | `["admin", "小开", "小"]` | ✅ |
| 5 | A2 多值触发匹配 | `any("arch" in "你好 @arch")` | True | ✅ |
| 6 | A2 无触发词静默 | `any("foo" in "普通消息")` | False | ✅ |
| 7 | A3 前缀剥离正确 | `starts("小开")` → `"收到"` | 匹配"小开"非"小" | ✅ |
| 8 | A4 @mention 路由 | `"@arch" in content` | 路由到 lobby | ✅ |
| 9 | 边界：空字符串 | `"".split(";")` | `[]` | ✅ |
| 9b | 边界：全空格 | `"  ;  ".split(";")` | `[]` | ✅ |

## 验证方法

纯逻辑验证（Python 模拟 `_mention_keywords` 行为），覆盖：
- 初始化解析（A1）
- 触发检查（A2）
- 前缀剥离（A3）
- 频道路由（A4）
- 边界情况（空值、空格）
- 长词优先排序防御
