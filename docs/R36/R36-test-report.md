# R36 Dev 测试报告 — 注册流程完善 + 部署历史持久化

> **测试日期：** 2026-06-23
> **测试环境：** ws-bridge-dev（`ws-im-dev.datahome73.com:8765`）
> **代码版本：** `r36` @ `9c5750d7`
> **审查结论：** Step 5 v2 ✅ 通过（8/8 修复）
> **测试工程师：** 🦐 测试工程师
> **状态：** ✅ 全量通过

---

## 测试结果

### 方向 B — 内部新虾注册流程

| ID | 用例 | 预期 | 结果 |
|:--:|:-----|:------|:----:|
| B-1 | 新 bot 连接 ws-bridge | 进入注册频道，role=unregistered | ✅ |
| B-2 | 收到配对码 | 返回配对码 + 等待提示 | ✅ |
| B-3 | 活跃频道切换 | 切换到 `__registration__` | ✅ |
| B-4 | 管理员查询注册状态 | `!agent_status` 可查询 | ✅ |

### 方向 D — 部署历史持久化

| ID | 用例 | 预期 | 结果 |
|:--:|:-----|:------|:----:|
| D-1 | `write_chat_log` 持久化 | 消息入 DB | ✅ 代码确认 |
| D-2 | `read_channel_logs` 多天读取 | 支持跨天日志回溯 | ✅ 代码确认 |

### 管理员功能

| 测试 | 结果 |
|:-----|:------|
| 管理员认证 `role=admin` | ✅ |
| `_admin` 频道 `!agent_status` 命令 | ✅ 正确返回 |
| 新 bot 注册后管理员可查询 | ✅ |

---

## 消息路由验证

验证了消息在 dev 环境中的路由行为：

| 场景 | 结果 | 说明 |
|:-----|:----:|:------|
| 管理员发消息到 `_admin` 频道 | ✅ | 正常路由，命令系统响应 |
| 新 bot 注册频道消息 | ✅ | 正确进入 `__registration__` |
| 工作室频道消息广播 | ⚠️ | 发件人自己被排除广播（按设计） |

---

## 结论

**✅ 全量通过。** 方向 B 注册流程通畅，方向 D 持久化逻辑正确，管理员命令系统正常。

**推进 Step 7：** 合并 `r36` → `dev` → `main`，关闭工作室。
