# R39 产品需求 — Bug 修复轮 + R38 功能验证

> **版本：** v1.0 ✅（项目负责人审核通过）
> **状态：** ✅ 已审核
> **产品经理：** 🧐 PM
> **日期：** 2026-06-24

---

## 0. 本轮定位

> **本轮不做新功能，只修遗留 Bug，并验证 R38 新功能是否全部正确运行。**

本轮性质：
- 纯 Bug 修复 + 回归测试
- 不引入新的功能方向
- 验收标准 = 所有 Bug 确认修复 + R38 验收标准复核通过

**本轮改动范围：** 根据 Bug 定位，可能涉及第①类（服务器代码）、第④类（Web 端）、或第⑤类（配置/部署）。不预设范围，以 Bug 根因决定。

---

## 1. Bug 清单

> **本轮性质：测试验证轮。** 以下 Bug A（F-8 消息重复）和 Bug B（F-7 下拉刷新）是 R38 已尝试修复的问题。R38 的修复可能已解决了它们，也可能没解决。
> 本轮的做法是：**先验证再决定要不要修**。如果验证通过 → 跳过修复，直接标记 ✅。如果仍然有问题 → 定位根因，修复后验证通过再闭环。
> Bug C 按 R38 测试报告中的发现问题同样处理。

### 🔴 Bug A — Web 端每条消息显示两遍

| 项目 | 内容 |
|:-----|:------|
| **编号** | F-8（TODO.md） |
| **严重度** | 🔴 P1 |
| **来源** | 项目负责人 2026-06-24 反馈 |
| **现象** | Web 聊天室中每条消息重复显示两次 |
| **猜测原因** | 发送者的原始消息 + 系统回显（echo）双重写入 |
|| **当前状态** | ❌ 仍未修复 — 已验证所有 Tab 均双倍显示（`loadMessages()` 轮询渲染无去重），F-8 确认需在本轮修复 |

**验证方法：**
1. 项目负责人在 Web 端查看消息，确认是否仍显示两遍
2. 如已修复 → 标记 F-8 为 ✅ 已完成并部署验证通过
3. 如仍重复 → 追溯 `save_message()` 和 `write_chat_log()` 两条写入路径，排查 `handle_api_chat` 的去重逻辑

### 🟡 Bug B — Web 端下拉刷新跳到大厅

| 项目 | 内容 |
|:-----|:------|
| **编号** | F-7（TODO.md） |
| **严重度** | 🟡 P2 |
| **来源** | 历史遗留（原 F-7） |
| **现象** | 有活跃工作室时下拉刷新，跳到大厅 tab |
| **当前状态** | ⏳ 待验证（R38 W-7 已要求「下拉刷新永远回到第一个 Tab（活跃）」，需先验证是否已部署生效。已解决则跳过，仍有问题则修复） |

**验证方法：**
1. 有活跃工作室时下拉刷新 → 期望回到活跃 Tab（第一个），不跳到大厅
2. 无活跃工作室时下拉刷新 → 回到大厅 Tab（第一个）
3. 如未生效 → 确认 templates.py 刷新逻辑是否已更新

### 🟡 Bug C — 其他 R38 运行中发现的问题

R38 开发管线（技术方案→编码→审查→测试）中可能发现了以下问题，需在 R39 统一修复：

| # | 事项 | 来源 |
|:-:|:-----|:------|
| C-1 | Task 状态命令的实际使用体验问题（如参数格式不顺手、响应不够明确） | R38 编码/审查/测试反馈 |
| C-2 | Web 进度 Tab 渲染或数据加载问题 | R38 Web 端编码反馈 |
| C-3 | Agent Card 配置加载异常或角色映射缺失 | R38 编码/部署反馈 |

> 具体条目以 R38 测试报告（`docs/R38/R38-test-report.md`）中的发现问题为准。

### 🔴 Bug D (F-9) — WS 重连后消息分流两边飞

| 项目 | 内容 |
|:-----|:------|
| **编号** | F-9 |
| **严重度** | 🔴 P1 |
| **现象** | 容器重启后各虾 WS 重连时 active_channel 未凝固到工作室，部分消息走了 lobby |
| **当前状态** | ❌ 已确认 — `_agent_active_channels.json` 正确但 bot 重连时 in-memory active_channel 不同步 |

### 🟡 Bug E (F-10) — 进度 Tab 空白

| 项目 | 内容 |
|:-----|:------|
| **编号** | F-10 |
| **严重度** | 🟡 P2 |
| **现象** | Web 端「📊 进度」Tab 显示「暂无任务进度数据」|
| **根因** | `!task_create` 不产出 `📊` 格式的进度消息，`renderProgressTab()` 按 `content.indexOf('📊') === 0` 过滤不到数据 |
| **当前状态** | ❌ 已确认 — 需修复 `!task_create` 或增加进度消息生成入口 |

### 🟡 Bug F (F-11) — Pipeline 信号死锁（Hot Standby 循环）

| 项目 | 内容 |
|:-----|:------|
| **编号** | F-11 |
| **严重度** | 🟡 P2 |
| **现象** | 各虾在工作室进入「等信号→无人发信号→继续等」的 Hot Standby 死循环，管线无自动推进机制 |
| **当前状态** | ⏳ 已记录 — 本轮若来不及修则排入下轮 |

---

## 2. R38 功能验证（回归测试）

R38 交付了以下功能，均需至少冒烟验证确认线上运行正常。

### 2.1 Task 状态机

| 验收项 | 验证方法 |
|:-------|:---------|
| TaskState 枚举 6 态完整 | `!task_create --context R39-test --step 1 --name "验证" --role admin-bot` 确认返回 task_id |
| 状态合法转换 | `!task_update --task-id <id> --state WORKING` → 允许；`COMPLETED → WORKING` → 拒绝 |
| INPUT_REQUIRED 流转 | 审查驳回 → 状态变为 INPUT_REQUIRED |
| reject_count ≥ 2 锁定 FAILED | 连续两次退回后第三次不可重提 |
| SQLite 持久化 | 重启服务后 Task 状态不丢失 |

### 2.2 Agent Card

| 验收项 | 验证方法 |
|:-------|:---------|
| Agent Card 配置文件加载 | 确认各角色 ID 和 display_name 正确 |
| `!task_create --role` 校验 | 传入不存在角色时返回错误，存在时正确路由 |

### 2.3 Web 端

| 验收项 | 验证方法 |
|:-------|:---------|
| 进度 Tab 显示 | Web 端可见「📊 进度」Tab |
| 表格 4 列完整 | Step、环节名称、工作人、状态 |
| 数据加载 | 任务进度数据能从服务端正常拉取 |
| Tab 排序 W-6 | 有活跃工作室时：活跃→大厅→管理员→📊 进度→历史 |
| 刷新规则 W-7 | 下拉刷新回到第一个 Tab |

---

## 3. 验收标准

| # | 验收标准 | 优先级 |
|:-:|:---------|:------:|
| **A-1** | Web 端每条消息不再重复显示，项目负责人确认 ✅ | P0 |
| **A-2** | 有活跃工作室时下拉刷新回到活跃 Tab（第一个），不再跳到大厅 | P1 |
| **A-3** | `!task_create / update / query / list` 四条命令全部可用且行为正确 | P1 |
| **A-4** | Task 状态转换矩阵合法转换通过、非法转换拒绝 | P1 |
| **A-5** | `INPUT_REQUIRED → WORKING` 的 reject_count ≥ 2 时锁定 FAILED | P1 |
| **A-6** | SQLite 持久化生效，服务重启后 Task 状态不丢失 | P1 |
| **A-7** | Agent Card 配置文件加载成功，所有角色 ID 和 display_name 正确 | P2 |
| **A-8** | Web 端进度 Tab 可见，表格 4 列数据正确加载 | P1 |
| **A-9** | Tab 排序为活跃→大厅→管理员→📊 进度→历史（有活跃工作室时）| P0 |
| **A-10** | 下拉刷新回到第一个 Tab | P0 |

---

## 4. 不纳入本次需求

| 不纳入项 | 说明 |
|:---------|:------|
| 新功能开发 | 本轮纯 Bug 修复 + 验证，不引入任何新方向 |
| P3 角色体系（F-3）| 已有工作区管理员机制可用 |
| 全平台管理面板（F-6）| 远大目标，本次不做 |
| Web 端 APK 封装（R36-2）| 独立方向，不进 Bug 轮 |
| 仓库改名/迁移（M 系列）| 独立方向 |

---

## 5. 参考文档

- [TODO.md](../TODO.md) — F-7 Web 端下拉刷新 Bug、F-8 消息重复 Bug
- [R38 产品需求文档](../R38/R38-product-requirements.md) — 需回归验证的验收标准
- [R38 测试报告](../R38/R38-test-report.md) — R38 测试中发现的附加问题
