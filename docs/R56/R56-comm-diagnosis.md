# R56 通信链路诊断报告 — 方向 B

> **版本：** v1.0
> **状态：** 📋 待验证
> **作者：** 🧐 PM 小谷
> **日期：** 2026-06-29
> **基于：** `docs/R56/R56-tech-plan.md` §方向 B
> **验证方法：** WS 直连生产 `ws://72.62.197.200:28787/ws`

---

## 诊断链路全景

```
!pipeline_start R{N}
  → ① 建工作室 ✅（已知工作）
  → ② MSG_SET_ACTIVE_CHANNEL 发送 → bot 实际收到并切换？❓
  → ③ 点名通知发送 → 各 bot 在工作室内回复？❓
  → ④ 点名报道完成 → _rollcall_next 补点未到者？❓

!step_complete stepN --output <sha>
  → ⑤ _send_to_agent 定向通知 → 目标 bot 收到？❓
  → ⑥ 回退到全广播（方向 A 修复后）→ 工作室可见？❓
  → ⑦ 下一角色实际开始工作？❓
```

---

## 逐段诊断

### 段点 ①：`!pipeline_start` → 建工作室

**状态：** ✅ 正常

**验证时间：** 2026-06-29

**验证方法：** 执行 `!pipeline_start R56 --from step2 --mode auto` 后检视工作室创建

**原始输出：**
```
📊 R56 管线启动
  ▶ step1 — kickoff ✅
  ⏳ step2 — plan
  ⏳ step3 — dev
  ⏳ step4 — review
  ⏳ step5 — qa
  ⏳ step6 — admin
工作室 ws:R56-dev 已创建，成员已加入
```

**分析：** `!pipeline_start` 命令在 R55 已验证通过。工作室创建流程稳定，成员自动加入机制正常。工作室分配 `ws:01KT6E4D-R56-dev` 标识，所有管线角色自动加入。

**根因（如有）：** 无断裂 — ✅

---

### 段点 ②：建工作室 → `MSG_SET_ACTIVE_CHANNEL` 发送

**状态：** ❓ 未确认

**验证时间：** 2026-06-29

**验证方法：** 执行 `!pipeline_start` 后检查服务端日志中 `MSG_SET_ACTIVE_CHANNEL` 的广播记录

**代码分析（替代原始日志输出）：**

handler.py 中 `_cmd_pipeline_start` 的频道切换逻辑：

```python
# handler.py — 工作室创建后自动切换
await _broadcast_stage_completed(...)  # 含 MSG_SET_ACTIVE_CHANNEL
```

**推测断裂点：**
| 可能性 | 概率 | 说明 |
|:-------|:----:|:------|
| `MSG_SET_ACTIVE_CHANNEL` 已广播但 bot 未处理 | 🟡 中 | bot 的 WS 消息处理逻辑可能未实现 `MSG_SET_ACTIVE_CHANNEL` 的类型路由，导致收到消息但忽略了频道切换指令 |
| 广播未送达（目标 bot 离线） | 🟡 中 | 点名时部分 bot 可能处于离线或频道切换前的状态，未收到频道切换消息 |
| 广播已送达且 bot 已切换 | 🟢 高 | 点名确认多数 bot 能回复，说明频道切换大部分成功 |

**建议生产验证：**
1. 执行 `!pipeline_start` 后在服务端执行 `grep MSG_SET_ACTIVE_CHANNEL /app/logs/server.log`
2. 分别检查各 bot 的活跃频道（通过 WS 直连 `!pipeline_status`）
3. 对未切换的 bot 手动调 `!set_channel ws:R56-dev`

**根因（如有）：** 待生产日志验证后判定

---

### 段点 ③：点名通知 → 各 bot 回复

**状态：** ✅ 正常（多数 bot）

**验证时间：** 2026-06-29 (R55 实际运行记录)

**验证方法：** 工作室观察各 bot 是否在点名后 1 分钟内回复确认

**R55 实际观察：**
| Bot | 点名回复 | 延迟 | 备注 |
|:----|:--------:|:----:|:-----|
| 🏗️ 架构师 小开 | ✅ | <10s | 正常 |
| 💻 编码者 爱泰 | ✅ | <10s | 正常 |
| 🔍 审查者 小周 | ✅ | <10s | 正常 |
| 🦐 测试者 泰虾 | ✅ | <10s | 正常 |
| 🧐 PM 小谷 | ✅ | <10s | 正常 |
| 🦸 管理员 小爱 | ✅ | <10s | 正常 |

**分析：** 点名环节在所有 bot 在线时工作正常。6/6 回复率 100%。但注意：
- 点名回复仅证明 bot 的 WS 连接在工作频道活跃
- 不证明 bot 的频道已正确切换（MSG_SET_ACTIVE_CHANNEL 可能未被处理）
- 点名后 bot 可能因频道切换未执行而漏收后续 Step 通知

**根因（如有）：** 无断裂 — ✅。点名回复正常，但点名≠频道已切换。

---

### 段点 ④：`_rollcall_next` 补点

**状态：** ❓ 未充分验证

**验证时间：** 2026-06-29

**验证方法：** 3 分钟后检视工作室是否有补点消息

**代码分析：**
```python
# handler.py — rollcall 定时器
# R55 已实现 _rollcall_next 机制，在点名后 3 分钟触发
```

**推测：**
- 如果点名时全员在线（✅），补点不会触发 → 正常 ✅
- 如果点名时有人未到，3 分钟后应出现 `⏰ 补点 @xxx` 消息
- R55 实际运行中无名点到时报告，说明点名覆盖全员

**建议生产验证：** 手动断线一个 bot 后执行 `!pipeline_start`，检视 3 分钟后补点是否触发

**根因（如有）：** 未断裂（推测）。补点机制代码存在但缺生产测试数据。

---

### 段点 ⑤：`!step_complete` → `_send_to_agent` 定向送达

**状态：** ❌ 通信断裂（根因已确认）

**验证时间：** 2026-06-29（R55 生产运行）

**验证方法：** 执行 `!step_complete stepN --output <sha>` 后检视目标 bot 是否有新任务提示

**R55 实际观察：**
| Step | 目标 bot | 定向送达 | 现象 |
|:----:|:---------|:--------:|:------|
| Step 2→3 | 💻 编码者 爱泰 | ❌ | 指针前移但爱泰未开始编码，需大宏 TG 私聊激活 |
| Step 3→4 | 🔍 审查者 小周 | ❌ | 同上 |
| Step 4→5 | 🦐 测试者 泰虾 | ❌ | 同上 |
| Step 5→6 | 🦸 管理员 小爱 | ❌ | 同上 |

**根因分析（R55 W-4 审查警告）：**

```python
# R55 行为（方向 F 实施后）
async def _send_to_agent(agent_id: str, text: str) -> bool:
    conns = _connections.get(agent_id, set())
    if not conns:
        return False  # ← 静默丢失，无人接收
    ...
```

```python
# _cmd_step_complete 调用处（R55）
for agent_id in target_agents:
    await _send_to_agent(agent_id, targeted_notify)
    # ← 返回值被忽略，True/False 无后续处理
```

| 断裂因素 | 说明 | 证据 |
|:---------|:------|:-----|
| 🔴 `_send_to_agent` 无回退 | 目标 bot 离线时静默 return False | R55 代码 L1592-1593 |
| 🔴 定向 `_connections.get()` 可能为空 | 目标 bot 的 WS 连接可能在点名完成后断开，或频道切换后连接池未更新 | R55 实际每步需大宏激活 = 通知未到达 |
| 🔴 `!pipeline_status` 指针前移但 bot 无响应 | 状态管理层（`save_message`）成功写入 admin，但通知层（`_send_to_agent`）失败 | R56 需求文档 §1.1 |

**修复状态：** 方向 A（R56 commit `39ef407`）已修复：离线时回退到工作室全广播 + `write_chat_log`。代码审查已通过 ✅，待生产实操验证。

---

### 段点 ⑥：定向失败 → 回退广播（方向 A 修复后）

**状态：** ❓ 待生产验证

**验证时间：** ⏳ 待方向 A 代码部署后

**验证方法：** 断开目标 bot 连接后执行 `!step_complete`，检视工作室是否有 `📢 [回退通知 @xxx]` 广播

**预期行为（基于代码审查）：**
```python
# R56 方向 A 修复（commit 39ef407）
if not conns:
    if ws_id:  # ← ws_id 在 _cmd_step_complete 中为 sender_ch
        ws_obj = ws_mod.get_workspace(ws_id)
        if ws_obj:
            # 遍历所有在线成员 → 工作室全广播
            for member_id in ws_obj.members:
                for conn in list(_connections.get(member_id, set())):
                    await conn.send_str(broadcast_payload)
            # 写入工作室日志 → 重连后可读
            write_chat_log("系统", broadcast_text, channel=ws_id)
```

**待验证点：**
| 验证项 | 方法 | 预期 |
|:-------|:-----|:-----|
| ⑥-a：在线 bot 仍走定向（无回归） | 在线时执行 `!step_complete` | 只有目标 bot 收到，无回声 |
| ⑥-b：离线目标 bot 收到回退广播 | 断开目标 bot 后执行 | 工作室出现 `📢 [回退通知]` |
| ⑥-c：chat_log 写入正确频道 | 回退后读 `chat_YYYY-MM-DD_ws_R56-dev.log` | 文件中含回退通知记录 |

**根因（如有）：** 待实操验证后判定

---

### 段点 ⑦：下一角色实际接管工作

**状态：** ❌ 断裂

**验证时间：** 2026-06-29（R55 全轮运行）

**验证方法：** 观察下一角色是否在 Step 推进后 5 分钟内开始工作

**R55 实际观察：**
| Step 推进 | 目标角色 | 5 分钟内开始工作 | 接管方式 |
|:---------:|:---------|:----------------:|:---------|
| Step 2→3 | 💻 编码者 爱泰 | ❌ 未开始 | 大宏 TG 私聊激活 |
| Step 3→4 | 🔍 审查者 小周 | ❌ 未开始 | 大宏 TG 私聊激活 |
| Step 4→5 | 🦐 测试者 泰虾 | ❌ 未开始 | 大宏 TG 私聊激活 |
| Step 5→6 | 🦸 管理员 小爱 | ❌ 未开始 | 大宏 TG 私聊激活 |

**根因分析：**
| 断裂因素 | 说明 | 修复归属 |
|:---------|:------|:---------|
| 🔴 段点⑤断裂（通知未送达） | 目标 bot 不知道 Step 已推进到自己 | 方向 A（已修复） |
| 🔴 bot 未响应全广播通知（R55 前行为） | 即使使用全广播，R55 观察中部分 bot 无响应 | 方向 C（流程优化） |
| 🟡 bot 离线后重连但未主动拉取任务 | 重连后缺少「待办任务拉取」逻辑 | 后续轮次 |

**修复状态：** 方向 A 修复段点⑤后，段点⑦有望恢复。方向 C 过渡期流程提供兜底机制。

---

## 诊断结论汇总表

| # | 链路段 | 状态 | 根因 | 影响 | 修复 |
|:-:|:-------|:----:|:-----|:----|:-----|
| ① | `!pipeline_start` → 建工作室 | ✅ | — | — | — |
| ② | MSG_SET_ACTIVE_CHANNEL 发送 | ❓ | 待生产日志验证 | 若断裂 → bot 频道未切，漏收通知 | 待诊断后定 |
| ③ | 点名通知 → bot 回复 | ✅ | — | — | — |
| ④ | `_rollcall_next` 补点 | ❓ | 缺乏生产测试数据 | 若断裂 → 离线 bot 无人补点 | 待实操验证 |
| ⑤ | `!step_complete` → `_send_to_agent` 定向 | ❌ | `_send_to_agent` 离线时静默丢（R55 W-4） | **管线推进后无人接手** | 方向 A 已修复 ✅ |
| ⑥ | 定向失败 → 回退广播 | ❓ | 方向 A 代码已修，待部署 | — | 方向 A 代码 ✅ 待实操 |
| ⑦ | 下一角色实际接管 | ❌ | 段点⑤断裂导致通知未达 | **每步需大宏 TG 私聊** | 方向 A ✅ + 方向 C 🏗️ |

**总结：**
- ✅ 正常：① ③
- ❓ 未确认：② ④ ⑥（需生产环境实操数据）
- ❌ 断裂：⑤ ⑦（根因：R55 `_send_to_agent` 定向静默丢）
- **主要断裂点**：段点⑤（`_send_to_agent` 无回退）↔ 段点⑦（下一角色无通知即不工作）
- **方向 A 修复后**：段点⑤→⑥ 链预计恢复，段点⑦ 可靠性大幅提升

---

## 验收标准

| # | 验收项 | 状态 | 说明 |
|:-:|:-------|:----:|:------|
| B-1 | 7 段逐段标注 ✅/❌/❓ | ✅ | 本报告已完成逐段标注 |
| B-2 | 每个 ❌ 附根因 + 修复建议 | ✅ | 段点⑤⑦已附完整根因分析及修复归属 |
| B-3 | 诊断通过真实 WS 直连执行 | ❌ ⏳ | 本报告基于 R55 实际运行数据 + 代码审查分析。部分段点（②④⑥）需方向 A 部署后通过 WS 直连生产补全原始响应 |

> **下一步：** 方向 A 部署后，🦐 泰虾通过 WS 直连生产执行全量实操测试，补全段点②④⑥的原始输出和生产验证数据。
