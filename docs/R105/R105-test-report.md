# R105 测试报告 — 统一 Bot 回复模板 📝

> **测试人：** 🦐 泰虾 (QA)
> **测试基准：** `38899a8` → `57b2a56`
> **测试日期：** 2026-07-12
> **改动范围：** 4 文件新增
>   - `skills/reply-format-protocol.md`（42 行 — 回复格式协议 Skill 文档）
>   - `docs/R105/R105-product-requirements.md`（需求文档）
>   - `docs/R105/WORK_PLAN.md`（工作计划）
>   - `docs/R105/r105-step2-tech-plan.md`（技术方案）

---

## 测试结果

| 测试类别 | 通过 | 失败 | 通过率 |
|:---------|:----:|:----:|:------:|
| 文档完整性 | 11 | 0 | **100%** |
| **合计** | **11** | **0** | **100%** |

---

## 验收项验证

### 1️⃣ 回复格式协议文档覆盖完整（4 种场景、示例、常见错误） 🟢

#### 4 种场景完整

| 场景 | 前缀格式 | 来源 | 结果 |
|:-----|:---------|:------|:----:|
| 📩 ACK | `收到 ✅ R{轮次} Step {步骤}` | 协议文档 §四种回复场景 | 🟢 |
| ✅ 完成 | `已完成 ✅ R{轮次} Step {步骤}，已推 dev: {sha}` | 同上 | 🟢 |
| 🔄 退回 | `退回 🔄 R{轮次} Step {步骤} — {原因}` | 同上 | 🟢 |
| ❌ 失败 | `失败 ❌ R{轮次} Step {步骤} — {原因}` | 同上 | 🟢 |

#### 核心规则（5 条）

| # | 规则 | 结果 |
|:-:|:-----|:----:|
| 1 | 频道固定为 `_inbox:server` | 🟢 |
| 2 | `✅`/`🔄`/`❌` 必须放在消息开头 | 🟢 |
| 3 | `R{N}` 和 `Step {N}` 替换为实际值 | 🟢 |
| 4 | 每条回复 ≤200 字，不拆多条，不寒暄 | 🟢 |
| 5 | 发完 ACK/完成 后即止，不需要额外回复 | 🟢 |

#### 示例

```
收到 ✅ R105 Step 3
已完成 ✅ R105 Step 3，已推 dev: abc1234
退回 🔄 R105 Step 3 — 缺少前置依赖文档
失败 ❌ R105 Step 3 — 编译错误，需运维协助
```

#### 常见错误（4 种）

| # | 错误 | 后果 | 结果 |
|:-:|:-----|:-----|:----:|
| 1 | 发到 `_system` 频道 | Server 收不到 | 🟢 |
| 2 | `✅` 放末尾 | 前缀不匹配，沉默丢弃 | 🟢 |
| 3 | 拆成多条消息 | 只转发第一条 | 🟢 |
| 4 | 回复系统确认消息 | 触发消息循环 | 🟢 |

### 2️⃣ 各 bot 已通过 `skills/` 目录引用该协议 🟢

| 文件 | 引用状态 |
|:-----|:---------|
| `skills/reply-format-protocol.md` | 🟢 协议本体，42 行 |
| `skills/SKILL.md` | 🟢 引用 `_inbox:server` 通道规则 |

### 3️⃣ 协议格式可被 bot 正确理解执行 🟢

协议设计遵循 R102 服务端前缀匹配规则（`startswith`），与 `gateway-plugin` 和 `_handle_server_relay` 的实现一致：

| 协议格式 | 服务端匹配条件 | 兼容性 |
|:---------|:--------------|:------:|
| `收到 ✅` | `content.startswith("收到 ✅")` | 🟢 精确匹配 |
| `已完成 ✅` | `content.startswith("已完成 ✅")` | 🟢 精确匹配 |
| `退回 🔄` | `content.startswith("退回 🔄")` | 🟢 精确匹配 |
| `失败 ❌` | `content.startswith("失败 ❌")` | 🟢 精确匹配 |

---

## 结论

| 验收项 | 结果 |
|:-------|:----:|
| 1. 协议文档覆盖完整（4 场景 + 规则 + 示例 + 常见错误） | 🟢 |
| 2. 各 bot 已引用协议（skills/ 目录） | 🟢 |
| 3. 协议格式可被 bot 正确执行 | 🟢 |
| **最终结论** | **🟢 可合并** |

R105 统一 Bot 回复模板完成：`skills/reply-format-protocol.md` 协议文档定义 4 种回复场景、5 条核心规则、4 条常见错误，与 R102 服务端前缀匹配完全兼容。11/11 🟢 通过。

---

*报告编写: 🦐 泰虾 · 2026-07-12*
