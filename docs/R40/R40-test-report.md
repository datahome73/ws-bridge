# R40 第④类 QA 验证报告

- **测试工程师:** 🦐 泰虾
- **审查工程师:** 🔍 小周
- **测试日期:** 2026-06-25
- **代码基线:** `9676eb97d3dee6db2c0aaad3e0734b1db1c1c924`（`origin/dev` HEAD）
- **测试类型:** 自动化（单元级）+ 手动（部署后）

---

## 1. 自动化测试结果 ✅ 5/5 通过

| # | 测试项 | 覆盖内容 | 结果 | 备注 |
|:-:|:-------|:---------|:----:|:-----|
| F-1/F-2 | JS 截断回归 + 重复路由删除 | 回归确认 | ✅ | 代码审查修复已验证 |
| **V-1** | 绑定码登录回归 | 生成→审批→轮询→页面渲染全链路 | ✅ | 无回归 |
| **V-4** | Cookie 持久化 | `validate_token` + `/chat` token 参数 | ✅ | token query + cookie 均可进入聊天页 |
| **V-7** | 服务重启不丢失 | `save`→`load`→`validate` 循环 | ✅ | 杀进程重启后旧 session 仍有效 |
| **V-8** | CSRF state 校验 | 无参数 400 / 伪造 state 403 / del 防重放 | ✅ | 全部通过 |

## 2. 黄灯项 🟡

| # | 测试项 | 结果 | 问题描述 | 风险评估 |
|:-:|:-------|:----:|:---------|:---------|
| **V-2** | GitHub 按钮条件渲染 | 🟡 | 按钮始终渲染在 DOM 中，无配置时点击返回 `501 GitHub OAuth not configured` | **低风险** — 按钮可见但点击安全降级 501，无崩溃风险。部署时配置 OAuth 后正常工作 |

## 3. 需部署后手动验证项 ⏳

| # | 测试项 | 依赖条件 | 验证方法 |
|:-:|:-------|:---------|:---------|
| **V-3** | 完整 OAuth 登录流程 | 真实 GitHub OAuth App + 公网域名 + `GITHUB_OAUTH_CLIENT_ID/SECRET/REDIRECT_URI` | 点击按钮→跳 GitHub 授权→回调→进入聊天页 |
| **V-5** | 身份映射生效 | `OAUTH_NAME_MAP` JSON 环境变量配置 | 映射表用户显示对应昵称 |
| **V-6** | 未映射用户可登录 | 同上 | 未映射用户回退显示 GitHub 用户名 |

## 4. 架构验证 🟢

- **双入口同步** ✅ — `web_viewer.py` 纯路由，不依赖 `__main__.py`/`entrypoint.py` 初始化顺序
- **Session 共存** ✅ — OAuth session 复用 `_web_sessions.json`，与绑定码 session 同存
- **安全矩阵** ✅ — State 防 CSRF + Cookie httponly + secure 按 scheme 切换 + token 不在日志泄露

## 5. 准出结论

```
✅ 5/5 自动化测试通过
🟡 1 黄灯（V-2，低风险，准出允许）
⏳ 3 项部署后验证（V-3/V-5/V-6）
```

**QA 签退人:** 🦐 泰虾
**审查签退人:** 🔍 小周
**总签退人:** 🦸 小爱
